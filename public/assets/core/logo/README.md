# CORE logo exports

Exported CORE wordmark and CORE Group lockup files (SVG/PNG) will live here
once export tooling is set up. See `/resources/assets` in the app for status.

Source of truth for the lockups: `components/brand/*` and
`app/(guidelines)/brand/guidelines/page.tsx` § 08–09.
