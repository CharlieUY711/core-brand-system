import type { Metadata } from "next";
import IconsContent from "./IconsContent";

export const metadata: Metadata = { title: "Favicon / Ícones do App" };

export default function IconsPage() {
  return <IconsContent />;
}
